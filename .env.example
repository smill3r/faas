# Generated by ./generate-cert.sh — run that script first, then edit this file.

SERVER_CERT="-----BEGIN CERTIFICATE-----
<your certificate here>
-----END CERTIFICATE-----"

SERVER_KEY="-----BEGIN PRIVATE KEY-----
<your private key here>
-----END PRIVATE KEY-----"

DOMAIN_NAME="localhost"

# API key for authenticating requests through APISIX.
# generate-cert.sh produces a random value automatically.
API_KEY="<random hex — set by generate-cert.sh>"

# Shared secret for NATS authentication.
# All services (api-server-*, runner) must present this token to connect.
# generate-cert.sh produces a random value automatically.
NATS_TOKEN="<random hex — set by generate-cert.sh>"
